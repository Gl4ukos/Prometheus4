ToDo List:
-> Reset the arm to "home" after detecting increasing deviation from target per move
-> Clamp NN response between two low values for delta theta
-> Avoid collisions
-> Add distance vector in training data
-> Add gradient descend after NN response, to fine tune movement